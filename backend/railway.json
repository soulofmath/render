{
    "build": {
      "nixpacksPlan": {
        "phases": {
          "setup": {
            "packages": ["python310", "gcc"]
          },
          "install": {
            "cmds": ["pip install -r requirements.txt"]
          }
        }
      }
    },
    "start": "uvicorn app.main:app --host=0.0.0.0 --port=8000"
  }
  